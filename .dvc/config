[core]
    remote = storage
    autostage = true

[remote "storage"]
    url = s3://your-bucket-name/dvc-storage

# For local storage (alternative)
# [remote "local"]
#     url = /path/to/local/dvc-storage
